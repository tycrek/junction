doctype html
html(lang='en')
	head
		meta(charset='UTF-8')
		meta(name='viewport', content='width=device-width, initial-scale=1.0')
		- const fancyTitle = `${data.package.name} - ${data.package.description}`;
		title= fancyTitle
		meta(name='description' content=data.package.description)
		meta(property='og:title' content=fancyTitle)
		meta(property='og:description' content=data.package.description)
		style
			!= css.tailwind
	body.w-full.h-screen.flex.flex-col.justify-center.items-center.gap-2.bg-neutral-800.text-neutral-100
		h1.text-5xl.font-bold.mb-8=data.package.name
		p.text-2xl.font-bold=data.package.description
		p.text-xl It is hosted on a Cloudflare Worker using Hono.js.
		p.text-xl This page is generated by my custom JAMstack generator, Pagery.